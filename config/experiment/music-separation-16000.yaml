# @package _global_

defaults:
    - _self_
    - override /datamodule: musdb18_16000
    - override /model: musdb_16000_model
    - override /trainer: allgpus

name: musdb18_16000  # experiment name

